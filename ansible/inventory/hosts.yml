---
all:
  children:
    proxmox:
      hosts:
        main:
        data:
      vars:
        ansible_user: root
        ansible_python_interpreter: /usr/bin/python3
    switches:
      hosts:
        icx7250:
          ansible_host: 192.168.10.1
