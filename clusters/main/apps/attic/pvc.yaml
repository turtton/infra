---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: attic-data
  namespace: attic
  annotations:
    data.longhorn.io/number-of-replicas: "2"
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
