---
apiVersion: v1
kind: Service
metadata:
  name: opencode-server
  namespace: opencode
spec:
  type: ClusterIP
  selector:
    app: opencode-server
  ports:
    - name: http
      port: 4096
      targetPort: http
      protocol: TCP
