apiVersion: v1
kind: Secret
metadata:
    name: opencode-server-secrets
    namespace: opencode
stringData:
    # Replace with your actual values before encrypting.
    # Then run: sops --encrypt --in-place server-secrets.sops.yaml
    password: "CHANGEME"
    GITHUB_TOKEN: "CHANGEME"
