apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: pod-restart
  namespace: picoclaw
rules:
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["get", "list", "delete"]
