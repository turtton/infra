name: skill-gitops
version: 1.2.0
description: "Auto-sync modified skills back to infra GitOps repo as PRs. Handles both interactive skill_manage edits and curator background consolidation."
hooks:
  - post_tool_call
