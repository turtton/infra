---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: hermes-home
  namespace: hermes
  labels:
    app: hermes-home
  annotations:
    secret.reloader.stakater.com/reload: "hermes-home-secrets"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hermes-home
  template:
    metadata:
      labels:
        app: hermes-home
    spec:
      serviceAccountName: hermes-home
      # Secret volumeのファイルは defaultMode: 0440 (owner r only) でマウントされるが、
      # fsGroup: 10000 (hermes ユーザーの GID) によりグループ読み取り可能になる。
      # これにより su hermes 経由でも /mnt/secrets/GITHUB_TOKEN を読める。
      # ref: https://kubernetes.io/docs/tasks/configure-pod-container/security-context/#configure-volume-permission-and-ownership-change-policy-for-pods
      securityContext:
        fsGroup: 10000
        seccompProfile:
          type: RuntimeDefault
      initContainers:
        - name: init-config
          image: busybox:latest
          command:
            - sh
            - -c
            - |
              sed 's|\${OPENAI_API_KEY}|'"$OPENAI_API_KEY"'|g' /template/config.yaml > /opt/data/config.yaml && \
              chmod 644 /opt/data/config.yaml
          env:
            - name: OPENAI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: hermes-home-secrets
                  key: OPENAI_API_KEY
          volumeMounts:
            - name: config-template
              mountPath: /template
            - name: data
              mountPath: /opt/data
        - name: init-skills
          image: busybox:latest
          command:
            - sh
            - -c
            - |
              # Kubernetes projected ConfigMap volumes use atomic mounts with symlinks.
              # cp -r copies symlinks as-is, leaving broken links under /opt/data/skills.
              # Use cp -rL to follow symlinks and re-seed each skill directory fresh.
              mkdir -p /opt/data/skills && \
              cd /skill-seed && \
              for src in *; do
                [ -e "$src" ] || continue
                rm -rf "/opt/data/skills/$src"
                cp -rL "$src" "/opt/data/skills/$src"
              done && \
              chmod -R 755 /opt/data/skills
          volumeMounts:
            - name: skill-seed
              mountPath: /skill-seed
            - name: data
              mountPath: /opt/data
        - name: init-plugin
          image: busybox:latest
          command:
            - sh
            - -c
            - |
              mkdir -p /opt/data/plugins/fluxer && \
              cp /plugin-seed/plugin.yaml /opt/data/plugins/fluxer/plugin.yaml && \
              cp /plugin-seed/adapter.py /opt/data/plugins/fluxer/adapter.py && \
              cp /plugin-seed/__init__.py /opt/data/plugins/fluxer/__init__.py && \
              chmod -R 755 /opt/data/plugins/fluxer
          volumeMounts:
            - name: plugin-seed
              mountPath: /plugin-seed
            - name: data
              mountPath: /opt/data
        - name: install-tools
          image: alpine:latest
          command:
            - sh
            - -c
            - |
              mkdir -p /opt/data/bin && \
              apk add --no-cache curl bash >/dev/null 2>&1 && \
              curl -sLO "https://dl.k8s.io/release/v1.32.0/bin/linux/amd64/kubectl" && \
              chmod +x kubectl && \
              mv kubectl /opt/data/bin/kubectl && \
              curl -sL "https://github.com/cli/cli/releases/download/v2.67.0/gh_2.67.0_linux_amd64.tar.gz" -o /tmp/gh.tar.gz && \
              tar xzf /tmp/gh.tar.gz -C /tmp && \
              mv /tmp/gh_2.67.0_linux_amd64/bin/gh /opt/data/bin/gh && \
              chmod +x /opt/data/bin/gh && \
              rm -rf /tmp/gh*
          volumeMounts:
            - name: data
              mountPath: /opt/data
      containers:
        - name: hermes
          image: docker.io/nousresearch/hermes-agent:latest
          imagePullPolicy: Always
          tty: true
          stdin: true
          command:
            - sh
            - -c
            - |
              set -eu
              unset GH_TOKEN GITHUB_TOKEN
              export GH_CONFIG_DIR=/run/gh-config
              mkdir -p "$GH_CONFIG_DIR"
              chown hermes:hermes "$GH_CONFIG_DIR"
              if ! su hermes -c 'GH_CONFIG_DIR=/run/gh-config /opt/data/bin/gh auth status' >/dev/null 2>&1; then
                su hermes -c 'GH_CONFIG_DIR=/run/gh-config /opt/data/bin/gh auth login --with-token < /mnt/secrets/GITHUB_TOKEN'
                su hermes -c 'GH_CONFIG_DIR=/run/gh-config /opt/data/bin/gh auth status' >/dev/null 2>&1 || { echo "FATAL: gh auth failed"; exit 1; }
              fi
              # Ensure /opt/data/bin is in PATH for terminal login shells.
              # bash -l sources /etc/profile which overrides the K8s-set PATH,
              # so ~/.profile restores /opt/data/bin before the terminal tool
              # captures its environment snapshot (init_session).
              mkdir -p /opt/data/home
              if ! grep -qxF 'export PATH="/opt/data/bin:$PATH"' /opt/data/home/.profile 2>/dev/null; then
                echo 'export PATH="/opt/data/bin:$PATH"' >> /opt/data/home/.profile
              fi
              chown hermes:hermes /opt/data/home/.profile
              exec /init /opt/hermes/docker/main-wrapper.sh gateway run
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            capabilities:
              drop:
                - "ALL"
              add:
                - SETUID
                - SETGID
                - CHOWN
                - DAC_OVERRIDE
                - FOWNER
                - SETPCAP
          env:
            - name: HERMES_HOME
              value: /opt/data
            - name: HOME
              value: /opt/data
            - name: PATH
              value: /opt/data/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
            - name: OPENAI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: hermes-home-secrets
                  key: OPENAI_API_KEY
            - name: OPENAI_BASE_URL
              value: "https://crof.ai/v1"
            - name: DISCORD_BOT_TOKEN
              valueFrom:
                secretKeyRef:
                  name: hermes-home-secrets
                  key: DISCORD_BOT_TOKEN
            - name: DISCORD_ALLOWED_USERS
              value: "275915497353117697,299106923741970432,280320003159490561"
            - name: DISCORD_REQUIRE_MENTION
              value: "true"
            - name: FLUXER_TOKEN
              valueFrom:
                secretKeyRef:
                  name: hermes-home-secrets
                  key: FLUXER_TOKEN
            - name: FLUXER_CHANNEL
              value: "1517888841577398272"
            - name: FLUXER_INSTANCE_URL
              value: "https://chat.turtton.net"
            - name: FLUXER_ALLOWED_USERS
              value: "1517827818073882624"
            - name: HERMES_DASHBOARD
              value: "1"
            - name: HERMES_DASHBOARD_BASIC_AUTH_USERNAME
              valueFrom:
                secretKeyRef:
                  name: hermes-home-secrets
                  key: DASHBOARD_USERNAME
            - name: HERMES_DASHBOARD_BASIC_AUTH_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: hermes-home-secrets
                  key: DASHBOARD_PASSWORD
          ports:
            - name: http
              containerPort: 9119
              protocol: TCP
          volumeMounts:
            - name: data
              mountPath: /opt/data
            - name: run
              mountPath: /run
            - name: gh-config
              mountPath: /run/gh-config
            - name: token-secret
              mountPath: /mnt/secrets
              readOnly: true
          resources:
            requests:
              cpu: 400m
              memory: 512Mi
            limits:
              cpu: 800m
              memory: 1Gi
          livenessProbe:
            exec:
              command:
                - sh
                - -c
                - "ps aux | grep -q '[h]ermes'"
            initialDelaySeconds: 60
            periodSeconds: 30
          readinessProbe:
            exec:
              command:
                - sh
                - -c
                - "ps aux | grep -q '[h]ermes'"
            initialDelaySeconds: 30
            periodSeconds: 10
      volumes:
        - name: config-template
          configMap:
            name: hermes-home-config
        - name: skill-seed
          projected:
            sources:
              - configMap:
                  name: hermes-config-ops-skill
                  items:
                    - key: SKILL.md
                      path: config-ops/SKILL.md
        - name: plugin-seed
          configMap:
            name: hermes-fluxer-plugin
        - name: run
          emptyDir:
            medium: Memory
        - name: gh-config
          emptyDir:
            medium: Memory
        # defaultMode: 0440 → owner(root) only read.
        # fsGroup: 10000 (Pod securityContext) により hermes ユーザーがグループ権限で読める。
        - name: token-secret
          secret:
            secretName: hermes-home-secrets
            defaultMode: 0440
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        storageClassName: longhorn
        resources:
          requests:
            storage: 5Gi
