apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: iceshrimp-valkey-data
  namespace: iceshrimp
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 1Gi
