## 変更内容

ULW-loopに**専用のplannerプロファイルを分離**し、計画立案（planフェーズ）を kimi-k3 + reasoning max で実行するようにしました。また、reviewerプロファイルのモデルを deepseek-v4-flash-0731 に更新しています。

### 1. plannerプロファイル新規作成

`home/profiles/planner/`

```yaml
model:
  provider: custom
  default: kimi-k3          # crof.ai で利用可能確認済み
  base_url: https://crof.ai/v1
  api_key: ${OPENAI_API_KEY}
agent:
  reasoning_effort: max
  toolsets: [kanban, memory, file]
```

- 計画立案・ゴール分解・acceptance criteria定義を担当
- `profile.yaml` に説明文を設定（後述）

### 2. reviewerモデル更新

`home/profiles/reviewer/config.yaml` — `glm-5.2` → `deepseek-v4-flash-0731`（crof.ai で利用可能確認済み）

### 3. 全プロファイルに説明文を追加（profile.yaml）

kanbanデコンポーザはタスク分解時に**プロファイル名+説明文のロースター**からアサイニーを選びます。説明文がないと「⚠ undescribed」扱いで名前だけで判断されるため、4プロファイル全てに役割説明を追加しました：

| プロファイル | 説明文 |
|------------|--------|
| orchestrator | 全体統括・ディスパッチ・ゴール管理 |
| planner | 計画立案・ゴール分解・acceptance criteria定義 |
| executor | 実装・実行 |
| reviewer | コードレビュー・検証 |

### 4. ULW-loopプラグイン v2.2.0

`plugins/ulw-loop/`

- `ulw_loop.py`: Kanbanタスク作成時のassigneeを `orchestrator` → `planner` に変更（定数も `ORCHESTRATOR_PROFILE` → `PLANNER_PROFILE` にリネーム）。planフェーズをplannerが主導
- ワークフロー記述・ヘルプ文言を「Plannerが計画立案 → 子タスク生成」に更新
- `plugin.yaml`: version 2.1.0 → 2.2.0

### 5. デプロイ基盤

- `kustomization.yaml`: `hermes-profiles` ConfigMap に planner.yaml + 各プロファイルの profile.yaml を追加
- `home-statefulset.yaml`: init-profiles のループに `planner` を追加、profile.yaml もシードするよう拡張

### 分離後の役割分担

| ロール | モデル | reasoning | 担当 |
|--------|--------|-----------|------|
| orchestrator | kimi-k2.7-code | — | 全体統括・ディスパッチ |
| **planner** | **kimi-k3** | **max** | **計画立案・ゴール分解・基準定義** |
| executor | glm-5.2 | max | 実装 |
| reviewer | deepseek-v4-flash-0731 | max | レビュー |

## テスト結果

- ✅ live環境で `hermes profile list` が planner: kimi-k3 / reviewer: deepseek-v4-flash-0731 を認識
- ✅ デコンポーザのロースター（`_build_roster`）に全4プロファイル+説明文が表示されることを確認
- ✅ プラグイン全モジュールの構文チェック、`PLANNER_PROFILE` リネーム確認
- ✅ `hermes plugins list` で ulw-loop 2.2.0 enabled 確認
- ✅ ConfigMap generator参照ファイルの存在・YAML構文検証
